<!--
<p style="color: green"><%= notice %></p>

<%= render @dish %>

<%= link_to "Back to dishes", dishes_path %>

<%if restaurant_signed_in? %>
  <%if current_restaurant.id == @dish.restaurant_id%>
    <div>
      <%= link_to "Edit this dish", edit_dish_path(@dish) %> 
      <%= button_to "Destroy this dish", @dish, method: :delete %>
    </div>
  <%end%>
<%end%>

<br><p>--------------</p> 
<%#counter risulta come stringa...%>
<%@counter = 0.to_s%>

<%if reviewer_signed_in? %>
  <div id="dishes">
    LISTA RECENSIONI: <br>
    <p>--------------</p> 
    
    RECENSIONI DI ALTRI UTENTI:<br>
    <% (Review.all).each do |review| %>
      <%if @dish.id == review.dish_id  && review.reviewer_id != current_reviewer.id%>
        <%= render review %>
        <p>--------------</p>
      <%end%>
    <% end %>

    
    <% (Review.all).each do |review| %>
      <%if @dish.id == review.dish_id  && review.reviewer_id == current_reviewer.id%>
        LA TUA RECENSIONE:<br>
        <%@counter = 1.to_s%>
        <%= render review %>
        <%= link_to "Edit this review", edit_review_path(review) %>
        <%= button_to "Destroy this review", review, method: :delete %>
        <p>--------------</p>
      <%end%>
    <% end %>

    <p>--------------</p>

  <%if @counter == 0.to_s%>
    <%= link_to "CREA NUOVA RECENSIONE", new_review_path %><br>
  <%end%>
    
  <p> <%#= link_to "Show this dish", @dish %> </p> 

  </div>

<%end%>
-->

<%= stylesheet_link_tag "piatto.css" %>

 <div id = "middle">
        <div id = "col1">
            <div id = "foto_piatto">
                <% i = 0 %>
                <% while @dish.images[i] != nil%>
                    <img src="<%= url_for(@dish.images[i])%>" alt="foto_piatto" width="250" height="250" id="dish_pic">
                    <%i += 1%>
                <%end%>
            </div>
            <div id = "actions">
                <%if reviewer_signed_in?%>
                    <div id = "review"><%= link_to "Lascia recensione", new_review_path, :class => "btn btn-warning btn-custom" %><br></div>
                <%elsif restaurant_signed_in?%>
                    <!-- Alert passato come parametro alla richiesta url di ricaricamento della pagina-->
                    <div id = "review"><%= link_to "Lascia recensione", dish_path(@dish.id, flash_alert: "Non puoi recensire un piatto"), :class => "btn btn-warning btn-custom"%><br></div>
                    <% flash.now[:alert] = params[:flash_alert] if params[:flash_alert] %>
                <%else%>
                    <div id = "review"><%= link_to "Lascia recensione", new_reviewer_session_path, :class => "btn btn-warning btn-custom" %><br></div>
                <%end%>
                <div id = "report">
                    <%if reviewer_signed_in? %>
                        <%= form_with url: new_report_path, method: :get do |form| %>
                            <%= form.hidden_field :report_id, value: @dish.id%>
                            <%= form.hidden_field :report_type, value:"dish"%>
                            <%= submit_tag "Segnala", :class => "btn btn-danger btn-custom" %>
                        <% end  %>
                    <%elsif (restaurant_signed_in?) and (current_restaurant == @dish.restaurant)%>
                        <%= link_to "Segnala", dish_path(@dish.id, flash_alert: "Non puoi segnalare un tuo piatto"), :class => "btn btn-danger btn-custom" %>
                        <% flash.now[:alert] = params[:flash_alert] if params[:flash_alert] %>
                    <%elsif (restaurant_signed_in?) and (current_restaurant != @dish.restaurant)%>
                        <%= link_to "Segnala", dish_path(@dish.id, flash_alert: "Non puoi segnalare un piatto perchè non sei un recensore"), :class => "btn btn-danger btn-custom" %>
                        <% flash.now[:alert] = params[:flash_alert] if params[:flash_alert] %>
                    <%else%>
                        <%= link_to "Segnala", new_reviewer_session_path, :class => "btn btn-danger btn-custom" %>
                    <%end%>
                </div>
            </div>
        </div>
        <div id = "col2">
            <div id = "plate_desc">
                <div id = "det_col1">
                    <div id = "plate_name"><%= @dish.name %></div>
                    <div id = "category">
                        <label id = "category_label">Categoria: </label>
                        <div id = "result"> <%= @dish.category%></div>
                    </div>
                    <div id = "rating">
                        <label id = "rating_label">Rating: </label>
                        <div id = "rating_score" class = "rating_score">
                            <div class = "value" id = "rating_value"> <%= @dish.avg_rating.round(2) %>/5</div>
                            <div class = "pb" id = "rating_pb">
                                <div class = "progress">
                                    <div class = "progress-bar progress-bar-striped bg-info" role = "progressbar" style="width: <%= @dish.avg_rating*20%>%" aria-valuenow="<%= @dish.avg_rating*20 %>" aria-valuemin="0" aria-valuemax="20"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div id = "det_col2">
                    <%if (@dish.restaurant_id != nil)%>
                    <div id = "restaurant">Ristorante : <%= Restaurant.find(@dish.restaurant_id).name %></div>
                    <%end%>
                    <div id = "price"> Prezzo : <%= @dish.price %> €</div>
                </div>
            </div>
            <div id = "reviews">
                <div id = "review_label"> Recensioni: <%= @dish.number_of_reviews %></div>
                <% (Review.all).each do |review| %>
                    <%if @dish.id == review.dish_id %>
                    <%= link_to review_path(review) do %>
                        <div id = "review_" class ="review">
                        <div id = "img_" class="img_review">
                            <img src="..." alt="Immagine del recensore">
                        </div>
                        <div id ="review_info_" class="review_info">
                            <div id = "username_and_rating_" class="username_and_rating">
                                <div id = "username_">
                                    <%=Reviewer.find(review.reviewer_id).username%>
                                </div>
                                <div id = "rating_no1" class = "rating_score">
                                    <div class = "value" id = "rating_value">
                                        <%= @avg_rate = ((review.rating1 + review.rating2 + review.rating3)/3) %>/5
                                    </div>
                                    <div class = "pb" id = "rating_pb">
                                        <div class = "progress">
                                            <div class = "progress-bar progress-bar-striped bg-info" role = "progressbar" style="width: <%= @avg_rate.round(2) * 20%>%" aria-valuenow="<%= @avg_rate.round(2) * 20%>" aria-valuemin="0" aria-valuemax="100"></div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div id = "rev_text_no" class = "rev_text">
                                <%= review.description %>
                            </div>
                        </div>
                    </div>
                    
                    <%end%>
                    <%end%>
            <% end %>
            </div>
        </div>
    </div>
